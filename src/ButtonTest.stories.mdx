import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

import ButtonTest from "./ButtonTest";

<Meta title="Design System/ButtonTest" component={ButtonTest} />

# Button Test

## Displays a button for test how this works

Just some small text about the description of the buttn test.
You can pass down children or the text prop.

<Story name="standard">
  <ButtonTest>Test</ButtonTest>
</Story>

### With a prop

<Story name="with prop">
  <ButtonTest text="This is a text" />
</Story>

### Playground

Experiment with this story with the Controls addon in the Canvas tab.

export const Template = (args) => <ButtonTest {...args} />;

<Canvas>
  <Story
    name="with Controls"
    args={{
      text: "test",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
